In order to run this programme, simply run the main.py script. If changes to the initialization of the environment or
population are required, always first run the initialize_environment script, and then the initialize_population script.

Data is shared between these scripts in csv files stored in the data folder.